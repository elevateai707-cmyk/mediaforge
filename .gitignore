.venv/
node_modules/
dist/
# Runtime library: the SQLite DB (plus its -wal/-shm sidecars), thumbnails,
# proxies and render scratch. Several GB — never committed.
data/
exports/
models/
__pycache__/
*.pyc
.env

# editor backups
*.bak
