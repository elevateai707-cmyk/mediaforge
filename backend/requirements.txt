# MediaForge backend dependencies (Python 3.12, Ubuntu 24.04)
# NOTE: torch/torchvision install from the DEFAULT PyPI index only. The default
# PyPI Linux wheels bundle the CUDA runtime, so CUDA on an RTX 3080 works out
# of the box; no custom index URLs are ever needed.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
sqlite-vec>=0.1.6
open-clip-torch>=2.26.1
faster-whisper>=1.0.3
insightface>=0.7.3
onnxruntime>=1.18.0
onnx>=1.16.0
opencv-python>=4.9.0.80
pillow>=10.3.0
pillow-heif>=0.16.0
imagehash>=4.3.1
scenedetect>=0.6.4
python-multipart>=0.0.9
websockets>=12.0
httpx>=0.27.0
python-dotenv>=1.0.1
numpy>=1.26.0
pytest>=8.0.0
pytest-asyncio>=0.23.0

# PyTorch — DEFAULT PyPI ONLY (bundles CUDA runtime for Linux wheels).
torch>=2.3.0
torchvision>=0.18.0
